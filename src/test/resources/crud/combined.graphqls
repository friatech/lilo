scalar Void

enum Role {
  ADMIN
  USER
}

type User {
    id: Int!
    name: String!
    age: Int!
    enabled: Boolean!
    role: Role!
}

type Queries {
    get(id: Int!): User!
    list: [User!]!
}

type Mutations {
    create(name: String!, age: Int!, enabled: Boolean!, role: Role!): User!
    delete(id: Int!): Void
}

schema {
    query: Queries
    mutation: Mutations
}
