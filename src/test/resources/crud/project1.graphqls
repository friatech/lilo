enum Role {
  ADMIN
  USER
}

interface UserRequest {
    name: String!
    age: Int!
    enabled: Boolean!
    role: Role!
}

type User implements UserRequest {
    id: Int!
    name: String!
    age: Int!
    enabled: Boolean!
    role: Role!
}

type Queries {
    get(id: Int!): User!
}

type Mutations {
    create(name: String!, age: Int!, enabled: Boolean!, role: Role!): User!
}

schema {
    query: Queries
    mutation: Mutations
}
