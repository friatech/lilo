scalar Void

type User {
    id: Int!
    name: String!
    age: Int!
    enabled: Boolean!
}

type Queries {
    list: [User!]!
}

type Mutations {
    delete(id: Int!): Void
}

schema {
    query: Queries
    mutation: Mutations
}
