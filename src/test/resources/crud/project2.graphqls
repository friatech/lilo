scalar Void

enum Role {
  ADMIN
  USER
}

type User {
    id: Int!
    name: String!
    age: Int!
    enabled: Boolean!
    role: Role!
}

type Queries {
    list: [User!]!
}

type Mutations {
    delete(id: Int!): Void
}

schema {
    query: Queries
    mutation: Mutations
}
